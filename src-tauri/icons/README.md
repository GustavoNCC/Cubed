# Iconos

Genera los iconos reales con:

```bash
npm run tauri icon path/al/logo.png
```

Esto crea `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns` e `icon.ico`
referenciados en `tauri.conf.json`. Son binarios y no se versionan en Fase 0.
